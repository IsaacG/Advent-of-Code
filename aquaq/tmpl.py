"""Aquaq Day N."""

import logging

log = logging.info

def solve(part: int, data: str) -> int:
    """Solve the parts."""
    lines = data.splitlines()
    if part == 1:
        return
    if part == 2:
        return
    if part == 3:
        return


TEST_DATA = """\
"""
TESTS = [
    # (1, TEST_DATA, None),
]
